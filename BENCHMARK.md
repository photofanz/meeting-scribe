# 本機會議轉寫 — 實測數據

機器：Apple M4 Pro / 48 GB RAM / macOS 26.5.2
量測日期：2026-07-29
所有數字都是實際跑出來的，沒有推估。

---

## 端到端速度

| 音檔長度 | 耗時 | 倍率 |
|---|---|---|
| 1 分 44 秒 | 9.6 s | 10.8× |
| 8 分 20 秒 | 55.8 s | 9.0× |
| **2 小時 00 分 08 秒** | **793.5 s（13 分 14 秒）** | **9.1×** |

2 小時那筆是完整程式碼、單獨執行、沒有其他工作競爭資源的乾淨量測。
講者判定正確（2 位），輸出為繁體。峰值記憶體 18.6 GB（max RSS 10.2 GB）。

短檔倍率被模型載入固定成本（約 4–6 秒）拉低，不代表長檔表現。

### 字級時間戳的實際代價（2 小時檔）

| 設定 | 耗時 | 講者判定 |
|---|---|---|
| 無字級時間戳、threshold 0.55（初版） | 598 s | 4 ✗（實際只有 2 人）|
| **有字級時間戳、threshold 0.75（現行）** | **793 s** | **2 ✓** |

多花 33% 的時間換到正確的講者歸屬。對 2 小時的會議來說是 10 分鐘 vs 13 分鐘，
而把 A 的話掛到 B 頭上在顧問會議記錄裡是不能接受的錯誤，所以這個交換划算。

## 元件個別速度（104 秒音檔）

| 元件 | 耗時 | 倍率 |
|---|---|---|
| ASR — mlx-whisper large-v3-turbo（Metal GPU），無字級時間戳 | 3.6 s | 29× |
| ASR — 同上，**開啟字級時間戳**（目前設定） | — | 約 20× |
| 聲紋分離 — sherpa-onnx + CAM++ zh（CPU 4 執行緒） | 3.9 s | 27× |

ASR 吃 GPU、聲紋分離吃 CPU，兩者**並行**執行，牆鐘時間約等於較慢的那個而非兩者相加。
把兩者並行化讓 104 秒的檔從 23.1 s 降到 9.6 s。

---

## 為什麼選這些模型

### 講者聲紋模型：CAM++ zh（非 ERes2NetV2）

同一個 104 秒檔、相同準確度（都是 2 位講者、12 個 turn 全對）：

| 分段模型 | 聲紋模型 | 執行緒 | 耗時 | 倍率 | 判定講者數 |
|---|---|---|---|---|---|
| fp32 | ERes2NetV2 | 4 | 16.88 s | 6.2× | 2 ✓ |
| fp32 | ERes2NetV2 | 10 | 18.54 s | 5.6× | 2 ✓ |
| **fp32** | **CAM++** | **4** | **3.92 s** | **26.7×** | **2 ✓** |
| fp32 | CAM++ | 10 | 4.87 s | 21.4× | 2 ✓ |
| int8 | ERes2NetV2 | 4 | 17.42 s | 6.0× | 3 ✗ |
| int8 | CAM++ | 4 | 4.45 s | 23.5× | 4 ✗ |

結論：CAM++ 快 4.3 倍且準確度相同。int8 分段模型會把 2 個人切成 3–4 個，不能用。
執行緒開到 10 反而變慢（ONNX 執行緒爭用）。

### 分群閾值：0.75

用 2 人（20 分鐘）與 4 人（8 分 20 秒）兩組音檔交叉驗證：

| threshold | 2 人檔判定 | 4 人檔判定 |
|---|---|---|
| 0.45 | 6 ✗ | 8 ✗ |
| 0.55（原預設） | 3 ✗ | 7 ✗ |
| 0.65 | 2 ✓ | 5 ✗ |
| **0.75** | **2 ✓** | **4 ✓** |
| 0.85 | 2 ✓ | 3 ✗（過度合併）|

### 不要**無條件**強制指定講者人數

4 人音檔、80 個 turn 的 ground truth 比對：

| 設定 | 分群數 | 每段分派準確率 |
|---|---|---|
| 自動分群，threshold 0.75 | 4 | **74/80 = 92%** |
| 強制 `num_clusters=4` | 3 | 55/80 = 69% |

在這組乾淨 benchmark 上，無條件強制指定人數**會讓結果變差**。但真實線上會議後來出現
了 2 人切成 25 / 112 群的失敗案例，因此 production pipeline 現在採用的是：

- 先跑 auto clustering（這一章的最佳設定）
- **只有當結果與使用者填的 headcount 明顯不符時**，才做固定群數／左右聲道 fallback

也就是說，現在的策略不是「永遠 force」或「永遠不 force」，而是**條件式 fallback**。

### 切分邊界參數：維持 min_duration_on=0.3 / min_duration_off=0.5

frame-level（0.1 秒解析度）準確率：

| min_on | min_off | 分群數 | frame 準確率 |
|---|---|---|---|
| **0.3** | **0.5** | 4 | **89.6%** |
| 0.3 | 0.2 | 4 | 89.1% |
| 0.2 | 0.1 | 4 | 89.0% |
| 0.1 | 0.1 | 4 | 89.0% |
| 0.3 | 0.05 | 4 | 89.0% |

預設值已是最佳，不需調整。

---

## 為什麼開字級時間戳

Whisper 會把跨越換人點的話合成同一個 segment，例如：

> 「我們現在的方案設計是不是太複雜了。**確實太複雜。**」

前半是 A 講的、後半是 B 講的，但整段會被歸給同一人。
在顧問會議記錄裡把某人的話掛到另一個人頭上是不可接受的錯誤。

開啟字級時間戳後，可以在換人的字元邊界把 segment 切開。
代價：ASR 從 32× 降到 20×，但因為聲紋分離（約 22×）本來就是並行瓶頸，
整體牆鐘時間只增加約 10%。

`MIN_RUN_SEC = 0.6`：短於 0.6 秒的講者跳動視為分群雜訊，併回相鄰段落，避免逐字碎裂。

---

## 簡轉繁：用 s2tw 不用 s2twp

Whisper 已經轉出**台灣用語**，只是寫成簡體字：

```
数位转型 → 應該轉成「數位轉型」（不是「數位化轉型」）
应用程式 → 應該轉成「應用程式」（不是「應用程序」）
资料库的权限问题 → 應該轉成「資料庫的權限問題」
```

`s2twp` 會在字元轉換之上再套一層中國→台灣**詞彙表**，把已經正確的詞誤譯：

| 原文 | s2tw（採用） | s2twp（不採用） |
|---|---|---|
| 权限 | 權限 ✓ | 許可權 ✗ |
| 信息安全 | 資訊安全 | 資訊保安 ✗ |
| 对账 | 對賬 → 修正表補成「對帳」 | 對賬 ✗ |

所以做法是：`s2tw` 字元轉換 + `bin/zhtw.py` 裡一份人工檢查過的台灣商務／技術詞修正表。
遇到新的錯誤詞就往那份表裡加。

---

## 記憶體

2 小時音檔的峰值記憶體約 14.6 GB（max RSS 10.4 GB）。48 GB 機器沒問題，
但如果同時跑多個轉寫工作要留意。3 小時以上的錄音建議先切段。

---

## 已知限制

1. **講者標籤沒有姓名** — 只是聲紋分群（講者1／講者2），要靠內容與與會者名單推斷對應。
2. **同音錯字必然存在** — 例如「撥空→多空」「權限→全線」「續約→誓約」。
   LLM 清稿是必要步驟，不是可選。
3. **上述準確率是用 macOS TTS 合成語音量的**。合成語音的講者間是零間隔硬接，
   真實會議有自然停頓，邊界準確率預期會更好；但 TTS 語音也比真人乾淨，
   錯字率預期會更差。這些數字是參考值，不是保證。
